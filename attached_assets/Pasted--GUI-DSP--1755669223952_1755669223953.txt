Используй структуру из этого документа: 
Сгенерируй полный исходный код автотюна с GUI, DSP, выбором Key и Scale, режимами (Classic, Hard, AI), поддержкой CREPE/DDSP/Rubber Band Library, с подключением всех библиотек.
Напиши полностью:
* Все .cpp и .h файлы по структуре
* Все параметры и GUI
* Все включения (includes), namespaces, инициализацию
* Все функции —  даже если они 1000+ строк


🔢 ГЛАВНАЯ СТРУКТУРА ПРОЕКТА:
1. PluginProcessor         — Главная логика DSP-потока
2. PluginEditor            — Интерфейс пользователя (GUI)
3. PitchCorrectionEngine   — Алгоритмы обработки питча (классика и hardtune )
4. Parameters              — Список параметров: Speed, Amount, Key, Scale
5. Utils                   — Частотные преобразования
6. LookAndFeel             — Кастомный внешний вид GUI-элементов
7. PresetManager (опц.)    — Сохранение/загрузка пользовательских пресетов
9. AIModelLoader           — Загрузка CREPE/DDSP
10. ModeSelector           — DSP режимы (Classic, Hardtune, AI, AI Full)
И не забудь про библиотеку Rubber Band Library
🧠 ИСПОЛЬЗУЕМЫЕ БИБЛИОТЕКИ:
1. JUCE (основной фреймворк) и подготовь сборку через CMAKE минуя линукс вообще никак не нужен только для среды мак создать vst3 и au платины через команду терминала ./build_simple.sh
2. CREPE (AI pitch detection)
3. DDSP (AI синтез)
4. TensorFlow Lite / ONNX — для моделей 
5. Rubber Band Library Обязательно 
6. C++ 

📂 CPP / H ФАЙЛЫ:
И другие чтоб выжать максимум из возможностей библиотек 

🧪 ПАРАМЕТРЫ ПЛАГИНА:
- speed: скорость коррекции (float slider)
- amount: сила коррекции (float slider)
- mode: режим работы (Classic, Hard, AI ddsp )
- key: тональность (C, C#, ..., B)
- scale: лад (Major, Minor, Chromatic)

🎛 GUI ЭЛЕМЕНТЫ:
- SpeedSlider (Rotary)
- AmountSlider (Rotary)
- ModeSelector (ComboBox)
- KeySelector (ComboBox)
- ScaleSelector (ComboBox)
- Save/LoadPreset Buttons

- CREPE: AI-распознавание нот
- DDSP: AI-пересинтез звука

сделай максимально професионально крутой дизайн по типо antares автотюн или вейс тюн реалтайм